// Page Paiements partagée (accessible à tous les rôles depuis la navigation).
// Réutilise l'implémentation existante pour éviter toute duplication.
export { default } from '../beneficiaire/paiements/page'
